extends Area2D
# Commenting this out for now because it is broken - @DUDEbehindDUDE
#
#@onready var timer = $Timer
#
#
#func _on_body_entered(body: Node2D) -> void:
	#print(body.name)
	#body.get_node("CollisionShape2D").queue_free()
	#timer.start()
	#var tween = get_tree().create_tween()
	#tween.tween_property(Engine, "time_scale", 0.05, 0.25).set_trans(Tween.TRANS_EXPO)
	#
#
#func _on_timer_timeout() -> void:
	#Engine.time_scale = 1
	#get_tree().reload_current_scene()
